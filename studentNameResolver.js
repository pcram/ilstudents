/**
 * Service with a single function, resolveDisplayNames, that takes an array of
 * student objects and adds a displayName property to each student.
 */
angular.module( 'il.students.studentNameResolver', []);
